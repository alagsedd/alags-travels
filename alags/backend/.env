# Flask specific
SECRET_KEY='a_very_secret_flask_key_change_me'
FLASK_APP=app.py
FLASK_ENV=development # or production

# JWT specific
JWT_SECRET_KEY='a_very_secret_jwt_key_change_me'

# Database specific (example for PostgreSQL, adjust as needed)
# DEV_DATABASE_URL=postgresql://user:password@localhost:5432/alags_dev
# TEST_DATABASE_URL=postgresql://user:password@localhost:5432/alags_test
# DATABASE_URL=postgresql://user:password@host:port/database_prod

# For SQLite (default in development config if DEV_DATABASE_URL is not set)
# No specific .env variables needed for the default SQLite setup,
# but you can override the path if you want:
# DEV_DATABASE_URL=sqlite:///path/to/your/dev_db.sqlite3
# TEST_DATABASE_URL=sqlite:///path/to/your/test_db.sqlite3
