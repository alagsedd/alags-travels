# This file makes the 'backend' directory a Python package.
# It can be empty.

# You could also initialize the Flask app instance here
# or import parts of your application to make them available
# when the package is imported, but for now, empty is fine
# as app.py handles app creation.
